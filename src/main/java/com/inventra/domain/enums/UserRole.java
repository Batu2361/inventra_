package com.inventra.domain.enums;

public enum UserRole {
    /** Full system access. */
    ADMIN,
    /** Can book stock movements. */
    WAREHOUSE_MANAGER,
    /** Read-only access. */
    VIEWER
}
